# The .env file holds sensitive configuration variables and secrets that are used by the software. It contains environment-specific variables such as database credentials, passwords, API keys, and other sensitive information. The .env file should not be pushed to the production server, as it can contain confidential information.

# Development settings
MODE=development